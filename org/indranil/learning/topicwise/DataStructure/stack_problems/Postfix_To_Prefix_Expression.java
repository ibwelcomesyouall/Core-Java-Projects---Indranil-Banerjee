package org.indranil.learning.topicwise.DataStructure.stack_problems;

public class Postfix_To_Prefix_Expression {

	public static void main(String[] args) {
		// 1. Read the prefix expression from left to right, one character at a time.
		//2. If we read the operand then push it onto the stack. 3. If we read the operator then pop two operands. Call the first popped operand as OP2 and second popped operand as OP1 from the expression ‘Operator OP1 OP2’. Then push this string onto the stack. Call this expression as the prefix expression. 4. Go to Step 1 until the complete input is read.

	}

}
